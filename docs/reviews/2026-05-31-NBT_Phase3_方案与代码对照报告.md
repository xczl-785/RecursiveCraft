# RecursiveCraft NBT Phase 3 方案与代码对照报告

> 目标分支：`1.20.1`
> 对照基线：`docs/architecture/NBT_总体方案决议.md`
> 对照设计：`docs/architecture/共享库存抽象层.md`
> 文档日期：2026-05-31
> 文档定位：用于确认第一阶段运行时 NBT 方案的真实代码状态、已落地项、保留桥接项与后续收口建议

---

## 一、结论摘要

当前代码状态已经足以支撑以下阶段性结论：

1. `Phase 2` 第一阶段“单来源玩家背包路径”的运行时 NBT 主链已经落地。
2. 当前实现与既定总体方案在主线上保持一致：
   - 保留 `CraftingPlanner` 的 `Item` 级粗规划职责。
   - 将运行时材料身份切换为 `MaterialKey` 主导。
   - 将虚拟库存、匹配、正式执行桥接为一套统一链路。
3. 当前尚不能把状态扩大表述为“NBT 完整方案完成”。
4. 当前最适合进入 `Phase 3` 的工作，不是继续扩张功能面，而是对既有实现做正式对照、验证收口与桥接收窄评估。

---

## 二、与方案保持一致的已落地项

### 2.1 运行时材料身份模型已落地

以下对象已经按设计进入正式代码：

1. `MaterialKey`
   - 证据：`common/src/main/java/xczl/recursivecraft/runtime/material/MaterialKey.java`
2. `NormalizedMaterialPayload`
   - 证据：`common/src/main/java/xczl/recursivecraft/runtime/material/NormalizedMaterialPayload.java`
3. `MaterialIdentityNormalizer`
   - 证据：`common/src/main/java/xczl/recursivecraft/runtime/material/MaterialIdentityNormalizer.java`
4. `DefaultMaterialIdentityNormalizer`
   - 证据：`common/src/main/java/xczl/recursivecraft/runtime/material/DefaultMaterialIdentityNormalizer.java`

这说明当前运行时链路已经不再以 `Map<Item, Integer>` 作为唯一正式材料身份模型。

### 2.2 匹配语义与请求级结果已落地

以下对象和语义已经进入正式代码：

1. `MaterialMatcher`
2. `IngredientRequirement`
3. `CandidateMatchKind`
4. `RequestLevelKind`
5. `DefaultMaterialMatcher`

证据：

- `common/src/main/java/xczl/recursivecraft/runtime/match/MaterialMatcher.java`
- `common/src/main/java/xczl/recursivecraft/runtime/match/IngredientRequirement.java`
- `common/src/main/java/xczl/recursivecraft/runtime/match/CandidateMatchKind.java`
- `common/src/main/java/xczl/recursivecraft/runtime/match/RequestLevelKind.java`
- `common/src/main/java/xczl/recursivecraft/runtime/match/DefaultMaterialMatcher.java:56-58`

这说明“候选级匹配”和“请求级 `MISSING / UNSUPPORTED` 聚合”已经从设计进入实现。

### 2.3 共享库存抽象层的第一阶段主体已落地

以下抽象已经形成正式代码：

1. `InventorySource`
2. `InventoryView`
3. `VirtualInventorySnapshot`
4. `PlayerInventorySource`
5. `PlayerInventoryView`
6. `ResolvedExecutionPlan`
7. `ResolvedConsumption`

证据：

- `common/src/main/java/xczl/recursivecraft/runtime/inventory/InventorySource.java`
- `common/src/main/java/xczl/recursivecraft/runtime/inventory/InventoryView.java:11-15`
- `common/src/main/java/xczl/recursivecraft/runtime/inventory/VirtualInventorySnapshot.java`
- `common/src/main/java/xczl/recursivecraft/runtime/inventory/PlayerInventoryView.java:26-105`
- `common/src/main/java/xczl/recursivecraft/runtime/execution/ResolvedExecutionPlan.java:6-13`

这说明“从虚拟事务到正式执行”的桥梁对象与单来源玩家背包视图已经具备。

### 2.4 `TransactionCalculator` 已切换为 `MaterialKey` 主导的虚拟搜索路径

当前 `TransactionCalculator` 的关键语义已经和设计保持一致：

1. 递归键已经升级为 `MaterialRequestKey(Item, desiredKey)`
   - 证据：`common/src/main/java/xczl/recursivecraft/core/TransactionCalculator.java:35-39`
2. 虚拟库存已经是 `VirtualInventorySnapshot`
   - 证据：`.../TransactionCalculator.java:51-58`
3. ingredient 解析已经通过 `IngredientRequirement` 和 `MaterialMatcher` 进入运行时精确匹配
   - 证据：`.../TransactionCalculator.java:160-167`、`181-230`
4. recipe 候选采用快照克隆后试探，成功才提交虚拟库存推进
   - 证据：`.../TransactionCalculator.java:196-209`、`335-344`
5. 输出侧使用 `resolvedOutputs`
   - 证据：`.../TransactionCalculator.java:170-177`、`465-477`

这说明核心递归规划并未推翻原架构，而是按既定路线完成了运行时语义升级。

### 2.5 正式执行桥接与单来源原子性约束已落地

当前 `PlayerInventoryView` 已具备第一阶段设计要求：

1. 先 `planExecution()` 绑定真实槽位
2. 再 `commitExecution()` 做 revalidate
3. revalidate 失败不发生部分扣减
4. 输出插入失败走 fallback 掉落

证据：

- `common/src/main/java/xczl/recursivecraft/runtime/inventory/PlayerInventoryView.java:42-60`
- `common/src/main/java/xczl/recursivecraft/runtime/inventory/PlayerInventoryView.java:64-105`

---

## 三、当前仍保留的桥接项与折中实现

### 3.1 `CraftingTransaction` 仍保留部分 `Item` 聚合桥接，但边界已继续收紧

当前 `CraftingTransaction` 虽然已经新增：

- `materialNeeds`
- `resolvedOutputs`
- `unsupported`

但仍保留以下旧桥接：

1. `needs: Map<Item, Integer>`
2. `getProvides()` 兼容访问口

证据：

- `common/src/main/java/xczl/recursivecraft/data/CraftingTransaction.java:13-16`
- `.../CraftingTransaction.java:20-27`

评估：

- 旧的 `getNetDeltas()` 与 `execute(Player)` 已从代码中移除，事务对象不再承担旧执行职责。
- `provides` 已不再作为独立状态保存，而是由 `resolvedOutputs` 现算得出。
- `needs` 也已从“总需求镜像”进一步收紧为“仅 unresolved 的 item-level missing deficits”，不再记录已被虚拟库存满足的消费。
- 当前残留的是数据聚合层面的最小 `Item` 级桥接，而不再是独立输出状态源或总消费镜像。
- 这仍会影响对象边界清晰度，属于后续可继续收窄的残留。

### 3.2 `CraftingTaskExecutor` 仍有 `Item` 级目标产出桥接，但旧浅层缺料分析支路已移除

当前 `CraftingTaskExecutor` 的主执行路径已经接入：

- `PlayerInventoryView`
- `DefaultMaterialIdentityNormalizer`
- `DefaultMaterialMatcher`

证据：

- `common/src/main/java/xczl/recursivecraft/core/CraftingTaskExecutor.java:151-168`

但仍保留以下桥接或旧逻辑：

1. `NetChanges.provides` 仍按 `Map<Item, Integer>` 检查目标产出
   - 证据：`.../CraftingTaskExecutor.java:28-35`、`119-126`、`136-139`

评估：

- 第一项属于第一阶段可接受桥接，因为目标产物主动指定 NBT 还未进入本期范围。
- 旧的 `reportMissingMaterials()` 与 `InventoryUtils.snapshot()` 支路已经从主代码中移除，不再构成当前阶段的保留缺口。

### 3.3 对外失败语义已收口到既定请求级可见边界

设计对外只承诺用户可见语义：

1. `MISSING`
2. `UNSUPPORTED`

当前真实执行失败路径会把内部状态折叠为既定请求级可见语义：

1. `FAILED_REVALIDATION -> MISSING`
2. `FAILED_CONSUME -> MISSING`

证据：

- `common/src/main/java/xczl/recursivecraft/runtime/execution/ExecutionCommitResult.java:18-27`
- `common/src/main/java/xczl/recursivecraft/core/CraftingTaskExecutor.java:137-166`
- `common/src/test/java/xczl/recursivecraft/core/CraftingTaskExecutorNbtTest.java`

评估：

- 这一项已经按既定设计收口完成。
- 当前残留问题不再是用户可见失败语义本身，而是更深层的执行桥接与扩展边界。

### 3.4 `TransactionCalculator` 与 `PlayerInventoryView` 的快照逻辑重复已收口

当前 `TransactionCalculator` 与 `PlayerInventoryView` 已统一改为调用同一套快照构造逻辑：

1. `VirtualInventorySnapshot.fromInventory(...)`
   - 证据：`common/src/main/java/xczl/recursivecraft/runtime/inventory/VirtualInventorySnapshot.java:26-44`
2. `TransactionCalculator.snapshotPlayerInventory()`
   - 证据：`common/src/main/java/xczl/recursivecraft/core/TransactionCalculator.java:480-481`
3. `PlayerInventoryView.snapshot()`
   - 证据：`common/src/main/java/xczl/recursivecraft/runtime/inventory/PlayerInventoryView.java:27-28`

评估：

- 这一项已完成代码收口。
- 当前共享库存抽象层在“单来源玩家背包快照构造”上已经具备单一事实来源。

### 3.5 `MaterialMatcher.aggregate(...)` 已进入主流程，但 `RuntimeMatchClause` 仍为保留骨架

当前 `MaterialMatcher` 接口与 `RuntimeMatchClause` 对象已经存在，其中：

1. 请求级 `MISSING / UNSUPPORTED` 聚合已经接入 `MaterialMatcher.aggregate(...)`
2. `RuntimeMatchClause` 当前仍更接近预留骨架，生产侧尚未形成真正的 clause 输出链

证据：

- `common/src/main/java/xczl/recursivecraft/runtime/match/MaterialMatcher.java`
- `common/src/main/java/xczl/recursivecraft/runtime/match/DefaultMaterialMatcher.java:53-58`
- `common/src/main/java/xczl/recursivecraft/runtime/match/RuntimeMatchClause.java`
- `common/src/main/java/xczl/recursivecraft/core/TransactionCalculator.java:181-230`
- `common/src/main/java/xczl/recursivecraft/core/TransactionCalculator.java:328-360`
- `common/src/test/java/xczl/recursivecraft/core/TransactionCalculatorNbtTest.java`

评估：

- `matcher.aggregate(...)` 已不再停留在接口层，而是进入了真实请求级收口路径。
- 当前未完成收口的只剩 `RuntimeMatchClause` 本身仍未形成更完整的生产侧语义链。
- 这不影响第一阶段功能成立，但说明 matcher 体系距离设计终态仍保留一个明确后续入口。

### 3.6 `PlayerInventoryView.planExecution()` 仍属于第一阶段可工作桥接版

当前 `planExecution()` 会基于 `transaction.getMaterialNeeds()` 反推一个“单 `MaterialKey` requirement”，再逐槽扫描真实背包。

证据：

- `common/src/main/java/xczl/recursivecraft/runtime/inventory/PlayerInventoryView.java:42-60`

评估：

- 这条路径在当前第一阶段是可工作的。
- 但它并没有把搜索阶段更丰富的候选选择上下文直接带入执行桥梁。
- 因此它应被记录为第一阶段保留桥接，而不是误判为最终形态。

---

## 四、验证状态

### 4.1 自动化验证

当前已具备覆盖第一阶段主路径的核心测试类：

1. `TransactionCalculatorTest`
2. `TransactionCalculatorNbtTest`
3. `CraftingTransactionTest`
4. `CraftingTaskExecutorNbtTest`
5. `PlayerInventoryViewTest`
6. `ExecutionPrimitivesTest`

证据：

- `common/src/test/java/xczl/recursivecraft/core/TransactionCalculatorNbtTest.java`
- `common/src/test/java/xczl/recursivecraft/core/CraftingTaskExecutorNbtTest.java`
- `common/src/test/java/xczl/recursivecraft/runtime/inventory/PlayerInventoryViewTest.java`
- `common/src/test/java/xczl/recursivecraft/runtime/execution/ExecutionPrimitivesTest.java`
- `common/src/test/java/xczl/recursivecraft/data/CraftingTransactionTest.java`

当前结论：

- 自动化测试已经足以支撑“`Phase 2` 第一阶段单来源玩家背包路径完成阶段性落地”的判断。

### 4.2 手工验证

当前仓库中尚缺正式的 `Phase 2` 手工验证复验记录。

当前结论：

- 手工验证要求已经在路线图中定义。
- 但要把 `Phase 3` 作为正式收口阶段关闭，仍建议补齐可执行清单与复验记录。

---

## 五、Phase 3 收口建议

### 5.1 当前应优先推进的收口项

1. 补齐 `Phase 2` 手工验证清单与复验记录
2. 评估 `CraftingTransaction` 中剩余最小 `Item` 级桥接是否还需要继续下压
3. 明确 `RuntimeMatchClause` 保留为下一阶段入口，而不继续在 `Phase 3` 内扩张
4. 把当前“设计已承诺但代码仍有桥接”的点单独登记为后续收窄项

### 5.2 当前不建议混入的内容

1. 不在 `Phase 3` 收口阶段直接推进外部存储实现
2. 不在 `Phase 3` 收口阶段直接推进目标产物 NBT 主动指定
3. 不在 `Phase 3` 收口阶段直接推进 planner 的 NBT 感知扩展

---

## 六、后续扩展优先级

已确认后续扩展优先级为：

1. 目标产物 NBT 主动指定
2. 原版相邻容器支持
3. 第三方存储兼容策略
4. planner 是否需要局部感知 NBT

外部存储路线已确认优先顺序为：

1. 先原版相邻容器
2. 后第三方存储网络

---

## 七、总评

当前代码状态可以概括为：

- 主路线正确。
- 第一阶段核心目标已达到阶段性完成标准。
- 旧执行路径、旧缺料分析支路和对外失败语义已完成一轮代码收口。
- 仍存在少量最小 `Item` 级桥接，以及被明确保留到下一阶段的 `RuntimeMatchClause` 骨架问题。

因此，当前最合理的直接任务不是继续扩张实现面，而是：

1. 完成 `Phase 3` 的正式收口
2. 固化验证与偏差记录
3. 再按已确认优先级进入下一轮扩展规划
