# RecursiveCraft 协作说明

本文档用于后续窗口、协作者或自动化代理在本仓库中继续工作时的快速对齐。当前内容以 `1.20.1` 分支状态为基准。

---

## 一、项目概况

RecursiveCraft 是一个递归合成辅助 Mod。当前核心能力是：

- 基于已知配方为目标物品规划递归合成路径
- 根据玩家当前持有材料执行递归合成
- 提供手持模式与方块模式入口
- 兼容 JEI 的指定配方触发

当前代码已经完成一轮以稳定性、线程安全、配置清理、GUI 数据层拆分和国际化为主的重构，并已在本地 `1.20.1` 分支完成回迁验证。

---

## 二、当前分支状态

### 2.1 已确认事项

- `1.21.1` 分支中的重构线实际仍基于 `Minecraft 1.20.1`
- 该重构线已在本地 `1.20.1` 分支完成快进合并验证
- 本地 `1.20.1` 分支当前位于 `8e90d84`
- 远端 `origin/1.20.1` 仍停留在 `5662632`

### 2.2 构建状态

- 仓库原始状态缺少 `gradle/wrapper/gradle-wrapper.jar`
- 因此直接执行 `./gradlew.bat build` 会失败
- 已使用临时下载的 Gradle 8.4 分发包在本地运行过 `build`
- 该次 `build` 返回成功退出码，但存在 Forge remap 警告

后续如果要把“可直接构建”作为正式交付条件，应补齐 wrapper 或明确使用系统级 Gradle 的方案。

---

## 三、代码现状判断

### 3.1 当前主要优点

- 递归规划核心已完成线程安全修复
- 请求参数校验与配方过滤较旧版本更完整
- GUI 侧已从单类堆积转向数据层分离
- 测试基础较旧版本更好

### 3.2 当前主要限制

- 库存模型仍然以 `Map<Item, Integer>` 为中心
- 材料匹配仍默认“同一 `Item` 全部可互换”
- 大量依赖 NBT 的 Mod 物品场景会被卡住
- 方块模式尚未拥有自己的库存或外部存储模型

---

## 四、当前优先议题

结合近期调研，当前建议的优先事项如下：

1. 先建立共享库存抽象层
2. 在抽象层上优先落地 NBT 支持
3. 再基于同一抽象层推进方块模式的存储支持

不建议的顺序：

- 直接在旧的 `Map<Item, Integer>` 模型上堆叠 NBT 特判
- 直接在旧模型上扩写相邻箱子或第三方存储支持

原因是这两种做法都会放大后续返工成本。

---

## 五、文档入口

后续继续该主题时，优先阅读以下文档：

1. `docs/README.md`
2. `docs/architecture/NBT_总体方案决议.md`
3. `docs/architecture/NBT_实施路线图.md`
4. `docs/plans/2026-05-31-shared-inventory-abstraction-design.md`
5. `docs/research/NBT_支持调研报告.md`
6. `docs/research/存储支持调研报告.md`
7. `docs/reviews/Code_Review_Report.md`
8. `docs/refactoring/Refactoring_Progress.md`

推荐阅读顺序：

- 先看 `docs/README.md` 获取分类与入口
- 再看 `architecture` 下的决议与路线图，确认当前目标和阶段
- 再看 `plans` 下的共享库存抽象层设计，确认实现输入
- 然后看 `research` 下的调研与机制梳理，补背景
- 最后看 `reviews` 与 `refactoring`，确认代码历史与遗留问题

---

## 六、后续实现原则

### 6.1 关于库存抽象

后续实现时，尽量避免新增以下模式：

- 直接在业务逻辑中遍历 `player.getInventory()`
- 新增更多 `Map<Item, Integer>` 快照
- 在多处分别实现“是否有材料”“如何扣减”“如何报告缺料”

目标应当是统一：

- 库存读取
- 材料匹配
- 虚拟预扣减
- 正式事务执行

### 6.2 关于 NBT 支持

第一阶段建议只承诺：

- 精确 `Item + NBT` 匹配
- 玩家背包链路先打通

不建议第一轮就做：

- 复杂的 Tag 忽略规则
- 大量可配置兼容例外
- 全量第三方存储网络兼容

### 6.3 关于存储支持

第一阶段建议只承诺：

- 方块模式可使用相邻原版容器

不建议第一轮就做：

- 手持模式远程读外部容器
- 跨房间或跨网络扫描
- 深度绑定某个第三方存储 Mod 的私有实现

---

## 七、协作建议

后续若继续开发该主题，建议采用以下流程：

1. 先确认工作分支是否基于 `1.20.1`
2. 先读本文件与两份调研报告
3. 先写抽象层设计，再写实现计划
4. 先做背包路径抽象化，再做 NBT
5. NBT 稳定后，再做方块模式存储支持

若中途需要判断优先级，默认以“先消除库存身份模型缺陷，再扩大库存来源范围”为原则。
